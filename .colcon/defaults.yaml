build:
  mixin:
    - compile-commands
    - ninja
    - ccache
    - debug
    - build-testing-on
  event-handlers:
    - console_direct+
  symlink-install: true
test:
  event-handlers:
    - console_direct+

